# Templates

This folder will hold renderer-owned template implementations.

For the MVP, implement only:

- `narrator_quote_punchline`

Template implementations should receive an edit recipe and asset registry entry. They should not call the LLM directly.
